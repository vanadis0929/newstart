import React, {
    Component
} from "react";
import ChatSearch from './presenter'
import Container from './index';

class Container extends Component {
    render() {
        return ( < ChatSearch / > )
    }
}

export default Container;